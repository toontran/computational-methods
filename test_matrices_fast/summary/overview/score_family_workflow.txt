Score-family workflow
======================

Date: 2026-04-28
Companion to: score_design_overview.txt, diagnostic_toolkit.txt

A "family" = one structural hypothesis about the slot-2/slot-r score
(e.g. rank-r lift, carry-confidence multiplier, soft gate). Variants
inside a family tune that hypothesis. Each family gets its own folder
under summary/ and runs through the steps below.


1. Folder layout
----------------

  summary/score_family_<name>/
    00_known_vs_unknown.md          # 1-pager: settled / open / toolkit gaps
    01_family_design.md             # the family + variant table
    02_gradient_theory.md           # shared ascent / Stiefel infra
    baseline/                       # frozen reference outputs (S6 etc.)
    variants/<variant_name>/
      spec.md                       # exact score, hyperparams, prediction
      diagnostics/                  # T1 grad-check, T2 per-block
      bench/                        # T3 streaming bench
      synthesis.md                  # ship / iterate / kill
    03_cross_variant_synthesis.md   # written after the round closes


2. Step order (per family)
---------------------------

  (1) Current understandings — pin in 00_known_vs_unknown.md from the two
      overview files. Don't re-dump; reference and summarize.
  (2) Lack of understandings — list with the diagnostic that would resolve
      each (Q1..Q6 in score_design_overview.txt §7, gaps a..j in
      diagnostic_toolkit.txt §8).
  (3) New family idea — one structural hypothesis. Write 01_family_design.md
      with the variant table BEFORE coding.
  (4) Variants within the family — adjustments to the hypothesis. Each gets
      a spec.md with exact score + prediction.
  (5) Gradient ascent theory — shared infrastructure in 02_gradient_theory.md
      (tangent projection, retraction, FD check). Build ONCE, variants plug in.
  (6) Implement scores — per variant, on top of the shared infra.
  (7) Benchmarks and component diagnostics — T1 (grad), T2 (per-block),
      T3 (streaming). Diff against baseline/ to "notice changes."
  (8) Further understandings — write 03_cross_variant_synthesis.md and
      update score_design_overview.txt §7 with Q-resolved/refuted pointers.


3. Discipline rules
-------------------

  - Snapshot a frozen baseline into baseline/ before launching variants.
    Otherwise "notice changes" has no reference.
  - Build the gradient infra once, before parallel variant work.
    Otherwise each variant re-derives subtly-different grads.
  - Parallel agents on variants only if specs are frozen and independent.
    Centralize the proposal step; parallelize implementation.
  - Each variant ends with a kill/ship/iterate decision in its synthesis.md.
    No open-ended variants left dangling.
  - When the family closes, append a one-liner to score_design_overview.txt
    §7 marking the Q resolved/refuted, with a pointer to the family folder.
    Otherwise the overview drifts from the actual state of the world.


4. Acceptance bar (inherits from diagnostic_toolkit.txt)
---------------------------------------------------------

  T1 grad-check:  rel_err < 1e-7 at float64
  T2 per-block:   oracle ranks above hm_triplet_raw on every probed block
  T3 streaming:   cos[1]² ≥ 0.6 on static-cex AND mixed-tail-sharp;
                  ≥ 0.7 on diffuse-diffuse; tail_mass ≤ 0.5 on ≥2 of 3
  Operational:    match-or-beat future_hmean_online (VALUE-ONLY rerun;
                  see INFRA-10) on the §6 table of score_design_overview.
                  txt (the strict T3 bar above is the MINIMUM; online
                  clears it comfortably on the 3-matrix gauntlet). A
                  variant that clears T3 but trails value-only online by
                  0.3+ on cos1² has not closed the real gap.

                  REGIME NOTE (2026-04-28, under §1ter of
                  score_design_overview.txt). The strict 3-matrix T3 bar
                  (static-cex, mixed-tail-sharp, diffuse-diffuse) and the
                  operational §6 table both sit in the HIGH-sample-
                  entropy regime — the score family's home turf where it
                  must be competent on its own merits. The LOW-sample-
                  entropy regime (residual-spiky-shocks, risk-residual-
                  panel) is conceded to iSVD/SVD by the complementarity
                  plan and is NOT in the ship-criterion: variants are
                  expected to be competitive there but not to beat iSVD.
                  Acceptance criteria that reference iSVD on the low-
                  entropy regime should be rewritten as stretch goals
                  (see FAM-02 below).

                  CAUTION (2026-04-28): the original `future_hmean_online`
                  numbers in summary/bench_matrix_sweep/synthesis.txt
                  are ORACLE-AWARE — its candidate pool contains
                  `q2_vs_q1oracle` built from V_exact[:,1] (see
                  half_window_sliding_hmean_experiment.py:24,247-250,490
                  and second_slot_tail_bias_diagnostic.py:32). Treat
                  those numbers as a CEILING, not as the operational
                  bar. The corrected value-only numbers live at
                  summary/bench_matrix_sweep_value_only_online/ once
                  INFRA-10 lands; until then, acceptance criteria
                  referencing "online" should be interpreted against
                  the value-only rerun, not the oracle-aware ceiling.


5. Backlog (snapshot 2026-04-28)
---------------------------------

Each item has: priority (P0–P3), effort (S/M/L), status (open / blocked /
in-progress / done / killed), depends-on (other backlog ids), resolves
(Q-numbers from score_design_overview.txt §7 / silent assumptions from
§2bis / toolkit §8 gaps), acceptance (the kill/ship criterion). Pick the
highest-priority unblocked item. Multiple agents can pull in parallel
when items have no dependency overlap.

Priority key:
  P0  do now — high value, unblocked, cheap or in critical path
  P1  next  — high value but blocked OR medium cost
  P2  later — moderate value or supporting item
  P3  parking lot — low yield given current evidence; revisit on signal

Status key:
  open         — ready to pick up
  blocked      — waiting on dependency
  in-progress  — assigned/being worked
  done         — closed; pointer in Notes
  killed       — closed without shipping; failure mechanism in Notes


--- AGGREGATOR ABLATIONS (in-family) ---

[AB-01] HM-vs-GM aggregator ablation
  Priority:    P0
  Effort:      S (~30 min)
  Status:      done / KILLED (2026-04-28)
  Depends on:  —
  Resolves:    Q7, §2bis (a.i)
  Acceptance:  T1 rel<1e-7 for GM3 grad; T2 oracle ≥ hm_triplet on
               every probed block; T3 cos1² compared to S6_HM3 on §6
               table. Ship if GM beats HM on any tail-dominant matrix
               in §6 by ≥0.05 cos1² without regressing others; kill
               otherwise but record numbers.
  Notes:       In-family — extend bench_matrix_sweep_r_sk_g_S6/
               synthesis.txt with S6_GM section. Outcome decides which
               aggregator FAM-01/FAM-03 inherit at rank-r.
               (2026-04-28) Acceptance RELAXED to within-family bar —
               the prior "≥50% of S6→online gap" formulation depended
               on `future_hmean_online` as an operational target, but
               online's pool is oracle-aware (see INFRA-10 / §4
               CAUTION). Aggregator ablations are intra-family choices
               and don't need an online-relative bar.
               (2026-04-28) DONE/KILLED — see
               summary/score_family_aggregator_ablation/synthesis.md.
               T1 PASS (rel ≤ 3.1e-10). T3: GM beats HM by ≥0.05 cos1²
               on diffuse-diffuse (+0.10) and mixed-tail-balanced
               (+0.05) but with cos0² regressions of −0.38 and −0.06
               (slot-1 lock destroyed). On 5 of 7 matrices GM either
               loses cos1² or regresses cos0²/other matrices.
               residual-spiky-shocks regresses cos1² by −0.28.
               Recommendation: FAM-01/03 inherit HM3.

[AB-02] F-norm vs operator-norm weighting ablation
  Priority:    P2
  Effort:      S
  Status:      partial (2026-04-28; T1+T2 done, T3 + synthesis pending)
  Depends on:  —
  Resolves:    §2bis (b.i)
  Acceptance:  Same harness as AB-01. Compare S6_HM3 with u_X =
               raw_X/σ_max(A_X)² on §6 table. Decide on op-norm only
               if it improves heavy-tailed matrices (static-cex)
               without regressing diffuse.
  Notes:       Cheap silent-assumption test; not yet promoted to a Q.
               PARTIAL 2026-04-28 (agent ran out of usage budget):
               - S6_OP variant added to r_sk_g_score.py (score + grad).
               - T1 grad-check PASS at float64 (rel ≤ 3.2e-10), see
                 summary/score_family_aggregator_ablation/
                 S6_OP_T1_gradient_check.log.
               - T2 per-block done on the 3-matrix probe set ×
                 5 blocks; outputs in summary/score_family_aggregator
                 _ablation/S6_OP_T2{,.csv,.json,_<matrix>.txt}. T2
                 columns sat_OP / score_OP populated.
               - T3 NOT RUN: no S6_OP-specific bench JSONs; the
                 mixed-tail-*_win64.json files in the folder belong
                 to AB-01's S6_GM run.
               - Synthesis NOT WRITTEN.
               To finish: rerun the 7-matrix sweep with
               --rsk-variant S6_OP into a distinct output filename
               pattern (e.g. {matrix}_S6_OP_win64.json), then write
               synthesis_S6_OP.md following the AB-01 template.
               (2026-04-28, 2nd attempt.) Stopped mid-run. Added the
               driver script summary/score_family_aggregator_ablation/
               aggregate_S6_OP.py (kept) and started 2 S6_OP runs
               (mixed-tail-balanced, mixed-tail-sharp); both .log
               files are 0-byte (killed before any output). Empty logs
               can be deleted on the next attempt. The driver script
               aggregate_S6_OP.py is a useful starting point for
               whoever finishes this — read it before re-running.


--- INFRASTRUCTURE (toolkit gaps) ---

[INFRA-01] Subspace alignment metric (principal angles)
  Priority:    P1
  Effort:      S
  Status:      done (2026-04-28)
  Depends on:  —
  Resolves:    toolkit §8 (a)
  Acceptance:  Function(V_opt, V_oracle) → array of r principal angles
               (cos² and radian). Wired into T2 alongside pairwise
               align_v?_proj. Documented in toolkit §1.
  Notes:       Prereq for any FAM-01 / FAM-03 work. Trivial via SVD of
               V_opt^T V_oracle.
               DONE 2026-04-28: utility lives at
               test_matrices_fast/subspace_metrics.py (principal_angles,
               principal_angles_pair; self-test via
               `python -m subspace_metrics`). Wired into r_sk_g_score.py
               T2 as per-row `pa1_o2` column AND per-block "subspace
               alignment" table over all rank-2 candidate frames
               (combined, sketch, mgain_svd, r_sk_g_S4/5/6/6_GM,
               r_sk_g_S5_sketch_init). Smoke output:
               summary/infra_subspace_alignment/smoke.txt (mixed-tail-
               sharp b6). Toolkit §1 updated. Unblocks INFRA-02,
               INFRA-04, FAM-01, FAM-03.

[INFRA-02] Stiefel FD gradient check
  Priority:    P1
  Effort:      M
  Status:      open (unblocked — INFRA-01 done b6f136a)
  Depends on:  INFRA-01
  Resolves:    toolkit §8 (d)
  Acceptance:  Column-wise FD with tangent-space projection; rel<1e-7
               on float64 for a known-correct trace form (sanity).
  Notes:       Unblocks FAM-01 / FAM-03 grad correctness.
               (2026-04-28) Attempted in this session, stopped mid-run.
               Got stuck investigating rel_err "marginally over 1e-9"
               on the sanity trace-form harness. No code was written
               to disk. Hypotheses for the next attempt:
                 (i)  Stiefel projection G_T = G - V·sym(V^T G) at
                      single precision in the projection arithmetic
                      can leak ~eps_mach into the tangent residual;
                      promote that step to float64 explicitly and
                      retry.
                 (ii) Default ε=1e-5 is too large for trace forms with
                      ‖G‖ of order m·n; try ε ∈ {1e-4, 1e-6} and pick
                      the trough of the U-shaped error curve.
                 (iii) Use Kahan/compensated summation on trace inner
                      products if the matrix dimension is large.
               If after those tweaks rel_err sits at 1-3e-9 on the
               trace sanity but ≪1e-7 on actual S6/S6_GM grads, the
               sanity test is the more stringent one (no division by
               u_X²); relax the trace-sanity bar to <1e-8 and treat
               the HM3/GM3 acceptance as the real bar.

[INFRA-03] Rank-r row-cheat baseline
  Priority:    P1
  Effort:      S
  Status:      done (2026-04-28)
  Depends on:  —
  Resolves:    toolkit §8 (e), (i)
  Acceptance:  "top-r rows of A_fut" candidate produced per-block,
               wired into T2 ranking. T2 STOP rule generalizes to
               "score(V_oracle) ≥ score(V_rowcheat) on every block".
  Notes:       Without this, the exploitability check breaks at r>1.
               DONE 2026-04-28: helper at
               test_matrices_fast/row_cheat_baseline.py
               (top_r_rows_frame, frame_score_S6, frame_score_S6_GM,
               oracle_frame_proj). Per-vector rowcheat_v1/rowcheat_v2 land
               in the candidate panel of r_sk_g_score.py and
               frob_hm3_score_diagnostic.py; frame-level acceptance line
               "Δ(oracle_frame − rowcheat_frame) [rank_r=...,oracle_r=...]:
               frame_S6=±... frame_S6_GM=±..." is emitted by both
               diagnostics. Smoke run on mixed-tail-sharp b12 at r=2 and
               r=3 in summary/infra_rank_r_row_cheat/smoke.txt.

[INFRA-04] Optimizer trajectory tracker
  Priority:    P2
  Effort:      M
  Status:      open (unblocked — INFRA-01 done b6f136a)
  Depends on:  INFRA-01
  Resolves:    toolkit §8 (b)
  Acceptance:  Per-iter log of score, principal-angles to oracle, and
               step direction; CSV per (matrix, block, variant).
  Notes:       Diagnoses drift on flat plateaus during FAM-01.
               (2026-04-28) Attempted in this session, stopped mid-run.
               Reportedly had a 30-iter single-cell prototype working
               in the agent's worktree but nothing committed to the
               main checkout — start fresh next attempt. Spec
               unchanged: tracked_ascend(value_grad_fn, V0, oracle_V,
               ...) with hooks emitting CSV rows per iteration.
               Reuse `subspace_metrics.principal_angles` for the cos²
               column. The synthesis should distinguish "lock-on then
               drift" (saddle-following) from "never lock-on" (random
               walk on level set) on mixed-tail-sharp / diffuse-
               diffuse — these have different fixes.

[INFRA-05] Plateau-width quantifier
  Priority:    P1
  Effort:      M
  Status:      done (2026-04-28)
  Depends on:  —
  Resolves:    Q2, toolkit §8 (c)
  Acceptance:  Sample N=200 random V's on Stiefel(d, r) within
               score ≥ τ·score(V_opt); plot principal-angles to oracle.
               Report mean/percentile angles per matrix.
  Notes:       Quantifies whether the rank-r lift actually shrinks the
               plateau — IF FAM-01 fails on T3, this tells us why.
               Done: plateau_width_probe.py (test_matrices_fast/).
               Outputs in summary/infra_plateau_width/. Synthesis at
               summary/infra_plateau_width/synthesis.md. Verdict on
               static-cex / mixed-tail-sharp / diffuse-diffuse at
               b6/12/31 (S6, r=1, τ=0.95, N=200): basin is "wide-but-
               multimodal-and-displaced", not narrow-around-oracle.
               Sample min_cos² clusters at 0–0.1 across all matrices;
               mixed-tail-sharp opt lands at min_cos²=0.000 at b31
               (catastrophic plateau drift). Acceptance rates 2.84–
               6.74% — pure rejection sufficed; MCMC fallback wired but
               unused. Establishes the r=1 baseline that FAM-01's
               plateau-shrinkage hypothesis must beat.

[INFRA-06] Carry-trajectory probe
  Priority:    P2
  Effort:      S
  Status:      done (2026-04-28)
  Depends on:  —
  Resolves:    toolkit §8 (g)
  Acceptance:  Per-block dump of s_top, sk_F2_low, spectral_concentration
               (s_1²/Σs_i²), carry-drift ‖V_t V_t^T − V_{t-1} V_{t-1}^T‖_F
               per matrix in the suite. CSV + summary plot.
  Notes:       Informs FAM-04 (carry-confidence multiplier shape).
               Run BEFORE FAM-04 picks a multiplier.
               DONE: probe at carry_trajectory_probe.py; outputs in
               summary/infra_carry_trajectory/ (CSV per matrix, win64,
               trajectories.png, synthesis.md). Three regimes identified:
               R1 fast-maturing carry (etf-basket-basis) → s_top² multiplier;
               R2 barely-settling (diffuse, spiky-shocks) → drift-based
               down-weight; R3 latching (static-cex, mixed-tail-*) →
               instantaneous-drift multiplier. See synthesis for the
               hybrid form recommended for FAM-04 first variant.

[INFRA-07] Component time-series
  Priority:    P2
  Effort:      S
  Status:      done (2026-04-28)
  Depends on:  —
  Resolves:    toolkit §8 (h)
  Acceptance:  For fixed candidates (oracle_v_proj, c_evi_v1, sketch_v1),
               plot (u_sk, u_g1, u_g2) per block per matrix.
  Notes:       Reveals when each component crosses the noise floor.
               Done — script: component_time_series_probe.py;
               outputs: summary/infra_component_time_series/
               ({matrix}_components.csv|png + synthesis.md). Panel
               extended to combined_v1 + mgain_svd_v1 per acceptance.
               Closes toolkit §6c gap and §8(h).

[INFRA-08] Hyperparameter sweep harness
  Priority:    P3
  Effort:      M
  Status:      open
  Depends on:  —
  Resolves:    toolkit §8 (f)
  Acceptance:  Grid-search wrapper over alpha/beta/gamma per variant
               across the matrix suite; CSV summary.
  Notes:       Currently hand-picking. Not blocking any current item.

[INFRA-09] Slot-r-specific stress probe
  Priority:    P3
  Effort:      M
  Status:      blocked (rank-r only)
  Depends on:  FAM-01 (or any rank-r work)
  Resolves:    toolkit §8 (j)
  Acceptance:  Run deflation-complement landscape probe at slot k=2..r;
               plot phi-drop and state-align constraint vs k.
  Notes:       Tests whether M1/M2 mechanisms scale with slot index.

[INFRA-10] Value-only online baseline
  Priority:    P0
  Effort:      S (~30 min bench)
  Status:      done (2026-04-28)
  Depends on:  —
  Resolves:    Discovered flaw 2026-04-28 — operational bar invalidation
  Acceptance:  Strip `q2_vs_q1oracle` from
               half_window_sliding_hmean_experiment.py:24 ONLINE_POOL
               (or expose as a `--online-include-oracle` flag, default
               False). Rerun the 7-matrix sweep at half_win=32. Output
               to summary/bench_matrix_sweep_value_only_online/ with
               the same file layout as bench_matrix_sweep_r_sk_g_S6/.
               synthesis.txt MUST include a side-by-side cos1² table:
               matrix | online (oracle-aware) | online (value-only) | Δ
               for the §6 matrices, so the size of the oracle leak is
               quantified.
  Notes:       The flaw: future_hmean_online's pool includes
               `q2_vs_q1oracle`, built from V_exact[:,1] projected into
               the row span via `raw_oracle_columns`
               (second_slot_tail_bias_diagnostic.py:32). 1 of 5 pool
               entries is the answer. Reported online numbers are an
               oracle-aware UPPER BOUND, not a value-only policy.
               Acceptance criteria across the backlog (AB-01, FAM-01,
               FAM-03, FAM-04, FAM-05, §4 op-bar, §5 conditional)
               formerly compared to those numbers; all are patched
               2026-04-28 to reference the value-only rerun. KEEP the
               oracle-aware numbers under summary/bench_matrix_sweep/
               as a labeled CEILING reference. Once value-only numbers
               land, propagate into the §6 table of
               score_design_overview.txt and §7 of diagnostic_toolkit.
               txt, then re-evaluate AB-01 / unblock FAM-01-3-4-5.
               DONE 2026-04-28: code change at
               half_window_sliding_hmean_experiment.py:31-33,512-514
               (pool split into VALUE_ONLY/ORACLE_AWARE; default flipped
               to VALUE_ONLY; --online-include-oracle CLI flag restores
               prior behavior). 8-matrix bench at half_win=32 written
               to summary/bench_matrix_sweep_value_only_online/
               {matrix}_win64.{json,csv,txt,log} + synthesis.txt.
               Headline: oracle leak Δcos1² = +0.77 on mixed-tail-
               balanced, +0.48 on diffuse-diffuse, +0.05 on residual-
               spiky-shocks; ≤±0.03 elsewhere. cos0² also leaked
               (+0.22-0.54 on tail-dominant). Reframed S6 vs value-only
               online: S6 WINS cos1² on mixed-tail-sharp (+0.063),
               mixed-tail-soft (+0.156), diffuse-diffuse (+0.081); ties
               on residual-spiky-shocks (-0.008) and risk-residual-panel
               (-0.004). Surviving real gaps: etf-basket-basis (-0.16)
               and the static-cex regime where combined dominates
               regardless. The "S6 trails online by 0.5-0.85" framing
               in the prior overview was substantially the oracle leak.
               §6 table of score_design_overview.txt should be
               re-propagated; FAM-01 acceptance is now reachable.


--- NEW FAMILIES ---

[FAM-01] Stiefel rank-r lift family
  Priority:    P1
  Effort:      L (3–5 days infra + 1–2 days per variant)
  Status:      blocked (depends on infra)
  Depends on:  INFRA-01, INFRA-02, INFRA-03, INFRA-10; baseline snapshot
  Resolves:    Q1 + Q2; absorbs Q5 (FAM-04 as subfamily)
  Acceptance:  Plateau-width (INFRA-05) shrinks with r on mixed-tail-
               sharp; T3 cos1² closes ≥50% of S6→(value-only online)
               gap on the §6 table. Ship the best variant; kill the
               family if the lift produces no plateau improvement.
  Notes:       Folder: summary/score_family_stiefel_rank_r/.
               Variants: B0 HM3-rank-r (or GM3 if AB-01 wins),
                         B1 HM3-rank-r + carry-confidence (= FAM-04),
                         B2 trace-form rank-r (= FAM-03 E0 wired in).
               If FAM-01 fails, pivot to FAM-03 as the structural fix.
               (2026-04-28) The "online" bar was tightened to refer to
               the value-only rerun (INFRA-10), not the original
               oracle-aware numbers. The plateau-shrinkage hypothesis
               is the true ship-criterion; the online comparison
               quantifies how much of the residual gap remains
               explainable by score-design choices vs. by the
               oracle-aware advantage that online enjoyed.

[FAM-02] Row-concentration guard family
  Priority:    P1 (was P0; demoted 2026-04-28 under §1ter framing —
               see Notes)
  Effort:      M (~1 day per variant)
  Status:      partial-D0 (2026-04-28; spec/T1/T2 done; T3 4/8 matrices;
               synthesis pending)
  Depends on:  —
  Resolves:    Q3 (F1); REFRAMED — primary target is the HIGH-sample-
               entropy regime as a within-window bias term on top of
               HM3, with spiky-residual as a bonus, NOT the other way
               around.
  Acceptance:  (REFRAMED 2026-04-28 under §1ter bias framing.) T2
               oracle ranking unchanged vs S6; T3 cos1² on the §6
               HIGH-sample-entropy matrices (mixed-tail-*,
               diffuse-diffuse, static-cex, etf-basket-basis) at least
               as good as S6 on every matrix, AND strictly better on
               at least one of the matrices where S6 currently fails
               (diffuse-diffuse 0.005, mixed-tail-sharp 0.013,
               mixed-tail-balanced 0.0004). Beating iSVD on residual-
               spiky-shocks / risk-residual-panel is a STRETCH GOAL,
               not the ship-criterion — that regime is conceded to
               iSVD/SVD by the complementarity plan. Ship best variant
               if it improves any high-entropy matrix without
               regressing others; kill if it produces no improvement
               on the high-entropy regime regardless of spiky-residual
               outcome.
  Notes:       Folder: summary/score_family_row_concentration_guard/.
               Variants: D0 S6 · relH1 multiplicative,
                         D1 S6 with leverage-weighted u_X (overlaps Q4),
                         D2 S6 with soft sigmoid gate on u_sk (F2 spec).
               INDEPENDENT of FAM-01 — can run in parallel.
               (2026-04-28 demotion P0 → P1.) Originally promoted P0
               because spiky-residual was treated as a regime the family
               must close. Under §1ter's bias framing, spiky-residual is
               iSVD's territory by design; the complementarity plan does
               not require the family to win there. relH1's REAL value
               is as the WITHIN-WINDOW STABILITY CHECK on the high-
               entropy proxy — i.e. a tail-conspiracy guard. The
               proxy "spread on visible sample → spread on population"
               can be fooled by a few rare large-norm rows that align
               with v constructively in the current window; HM3 catches
               the between-window cut of that pathology (conspiracy
               doesn't replicate from A_cur to A_fut), relH1 catches
               the within-window cut (a single row supplies most of
               ‖A_X v‖²). Both axes are needed on heavy-tailed matrices
               where conspiracies can persist across windows by chance.
               Demoted to P1 (not P0) because (i) the high-entropy
               failure on mixed-tail-sharp / diffuse-diffuse is also
               plateau-drift dominated (FAM-01's territory), not purely
               bias-stability dominated; (ii) D0 work is already partial
               and the existing T3 matrices include diffuse-diffuse and
               mixed-tail-* — finishing D0 directly tests the within-
               window stability hypothesis on the high-entropy regime.
               Keep D0 closure as an immediate todo; defer D1/D2 unless
               D0 surprises.
               PARTIAL D0 2026-04-28 (agent ran out of usage budget):
               - Family folder populated: 00_known_vs_unknown.md,
                 01_family_design.md, 02_gradient_theory.md, baseline/.
               - D0 spec at variants/D0/spec.md; D0 variant added to
                 r_sk_g_score.py with analytic grad (visible as `D0`
                 in --rsk-variant choices).
               - T1 grad-check PASS in variants/D0/diagnostics/
                 T1_grad_check.txt (rel ≤ 4e-10).
               - T2 per-block done on 3-matrix probe set in
                 variants/D0/diagnostics/T2_per_block{,.csv,.json,
                 _<matrix>.txt}.
               - T3 PARTIAL (4 of 7 matrices): diffuse-diffuse,
                 mixed-tail-balanced, mixed-tail-sharp, static-cex
                 have full {.json,.csv,.txt,.log}; etf-basket-basis,
                 mixed-tail-soft, residual-spiky-shocks,
                 risk-residual-panel have only .log (incomplete runs).
               - synthesis.md NOT WRITTEN.
               To finish: rerun the 4 missing T3 matrices with
               `--rsk-variant D0`, then write variants/D0/synthesis.md
               and 03_cross_variant_synthesis.md (D1 / D2 still
               spec-only — out of scope for D0 closure).
               (2026-04-28, 2nd attempt under reframed acceptance.)
               Stopped mid-run; added 0 new bench JSONs. Empty .log
               placeholders for etf-basket-basis, mixed-tail-soft,
               residual-spiky-shocks, risk-residual-panel are leftover
               from the killed runs and can be deleted on the next
               attempt without harm. Apply the REFRAMED acceptance
               from above (high-entropy regime focus; spiky-residual
               stretch-only) when writing synthesis.md.

[FAM-03] Subspace-trace family
  Priority:    P2
  Effort:      L (uses FAM-01 infra)
  Status:      blocked (depends on infra)
  Depends on:  INFRA-01, INFRA-02, INFRA-10
  Resolves:    Tests §2bis "HM-family hypothesis itself" critique
  Acceptance:  T1 grad correct; T2 oracle ranks above row-cheat at
               rank-r; T3 cos1² beats FAM-01 B0 on at least 2 of the
               §6 tail-dominant matrices. Ship if so; otherwise the
               HM aggregator is validated.
  Notes:       Folder: summary/score_family_subspace_trace/.
               Variants: E0 sum-of-trace (no per-v aggregator),
                         E1 HM-of-traces, E2 min-of-traces (smoothed).
               Run E0 in parallel with FAM-01 B0 once infra is built.

[FAM-04] Carry-confidence multipliers (subfamily of FAM-01)
  Priority:    P2
  Effort:      M
  Status:      blocked (depends on FAM-01 + INFRA-06)
  Depends on:  FAM-01, INFRA-06
  Resolves:    Q5, §2bis (a.ii)
  Acceptance:  Beats FAM-01 B0 on ≥1 tail-dominant matrix without
               regressing others.
  Notes:       Multiplier candidates: s_top², spectral concentration,
               carry-drift. Pick shape AFTER INFRA-06 reveals
               trajectories per matrix family.

[FAM-05] Rank-aware threshold
  Priority:    P3
  Effort:      M
  Status:      blocked (rank-r only)
  Depends on:  FAM-01
  Resolves:    Q6
  Acceptance:  Beats FAM-01 B0 on the §6 table.
  Notes:       u_sk = max(r·raw_sk/sk_F2_low − 1, 0). Sharper than S5
               per-row gate; meaningful only at rank-r.

[FAM-06] Per-row leverage / c-weighting
  Priority:    P3
  Effort:      M
  Status:      open
  Depends on:  —
  Resolves:    Q4, §2bis (b.ii)
  Acceptance:  Beats S6 on §6 table OR demonstrates equivalence-up-to-
               scale claim from §7 Q4.
  Notes:       Could be merged into FAM-02 D1 if scope stays narrow.


--- CURRENT SEQUENCING RECOMMENDATION ---

Not part of the backlog itself; a snapshot read on what to pull next.
The backlog is the source of truth and changes as items close.

(Reordered 2026-04-28 under §1ter of score_design_overview.txt — the
priority is now "high-sample-entropy regime first." Low-entropy /
spiky-residual work is conceded to iSVD by the complementarity plan
and is downstream.)

  Now (parallel; all unblocked, all targeting the high-entropy regime):
    INFRA-02         (P1, M, blocked-until-INFRA-01 ✓)  →  unblock FAM-01
    INFRA-04         (P2, M, blocked-until-INFRA-01 ✓)  →  unblock FAM-01
                                                          drift diagnosis
    FAM-02 D0 close  (P1, S, partial)                  →  finish 4 missing
                                                          T3 matrices,
                                                          write synthesis;
                                                          tests within-
                                                          window bias on
                                                          high-entropy
                                                          regime
    AB-02 finish     (P2, S, partial)                  →  T3 + synthesis
                                                          for op-norm
                                                          weighting

  Next (FAM-01 — the structural fix for the high-entropy regime):
    FAM-01 B0        (P1, L, blocked-until-INFRA-02)   →  rank-r lift,
                                                          THE high-entropy
                                                          fix for plateau
                                                          drift on
                                                          mixed-tail-sharp
                                                          / diffuse-diffuse
    FAM-03 E0        (P2, L, blocked-until-INFRA-02)   →  parallel to
                                                          FAM-01

  Conditional on FAM-01 B0 closing ≥50% of S6→(value-only online) gap
  on the high-entropy §6 matrices:
    FAM-04, FAM-05   (P2/P3)
  Conditional on FAM-01 failing plateau-shrinkage hypothesis:
    Promote FAM-03 to P1; deprioritize remaining FAM-01 variants.

  (2026-04-28) "online" everywhere in this section now means the
  value-only rerun from INFRA-10 — see §4 CAUTION. AB-01 (HM-vs-GM)
  is DONE/KILLED — keep HM3.

  What is NOT in this sequencing list (deliberately): any work whose
  primary acceptance is "beat iSVD on residual-spiky-shocks /
  risk-residual-panel". Under §1ter's bias framing that is iSVD's
  regime by design; the score family should be competitive there but
  not required to win. Pull such work in only after the high-entropy
  regime is solved, and only as a stretch goal.


6. Family handoff checklist
----------------------------

When a family closes (kill OR ship):
  - 03_cross_variant_synthesis.md written, includes T1/T2/T3 numbers
    diff'd against baseline/.
  - score_design_overview.txt §7 updated: mark each Q resolved /
    refuted / iterated, with a pointer to the family folder.
  - diagnostic_toolkit.txt §8 updated: any new infra built (subspace
    metric, Stiefel FD, etc.) becomes a permanent toolkit entry.
  - If the family killed a hypothesis (e.g. "rank-r lift doesn't shrink
    plateau"), add a §1bis-style entry to score_design_overview.txt
    documenting the verified failure mechanism so the next family
    doesn't re-do the same probe.
